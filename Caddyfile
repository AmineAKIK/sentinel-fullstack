{$CADDY_DOMAIN} {
	encode zstd gzip

	log {
		output stdout
		format console
	}

	# Frontend — toutes les requêtes sauf /api/*
	handle /api/* {
		reverse_proxy backend:3000
	}

	handle {
		reverse_proxy frontend:8080
	}
}
